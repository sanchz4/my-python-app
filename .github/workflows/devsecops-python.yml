name: Python DevSecOps Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  devsecops:
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Setup Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # 3. Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4. Lint code (non-blocking)
      - name: Lint with pylint (non-blocking)
        run: pylint app.py tests/*.py || true

      # 5. Run unit tests
      - name: Run unit tests
        run: pytest tests --maxfail=1 --disable-warnings -q

      # 6. Dependency vulnerability scan
      - name: Check for vulnerabilities
        run: safety check -r requirements.txt

      # 7. Optional: Build package (if setup.py exists)
      - name: Build Python package
        run: |
          python setup.py sdist bdist_wheel || echo "No setup.py, skipping build"
